name: twater
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/cover.png
    - assets/transparent_cover.png
    - assets/profile.png
    - assets/transparent_profile.png
    - assets/default.png
    - assets/bird.png
    - assets/fillbird.png
    - assets/fillbird.svg
